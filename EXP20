%{
#include <stdio.h>
%}

%%
//.*                         { printf("Single-line comment: %s\n", yytext); }
\/\*([^*]|\*+[^/])*\*+\/    { printf("Multi-line comment: %s\n", yytext); }
.*                          { printf("Not a comment: %s\n", yytext); }
%%

int main() {
    yylex();
    return 0;
}

int yywrap() {
    return 1;
}
