%{
#include <stdio.h>
%}
%%
[6-9][0-9]{9}   { printf("Valid Mobile Number: %s\n", yytext); }
[0-9]{10}       { printf("Invalid Start Digit: %s\n", yytext); }
.|\n            ;
%%
int yywrap() { return 1; }
int main() { yylex(); return 0; }
